# Deferred Work

Items logged here were surfaced during code review but deferred as pre-existing, out-of-scope, or not actionable at the time of review.

---

## Deferred from: implementation of 3-4-assign-damage-to-unit (2026-07-05)

- **`Hero` combat wound-state is not restorable by undo in a no-reveal rampager combat** — `Hero` is deliberately excluded from `GameStateSnapshot`/`TakeSnapshot`/`RestoreSnapshot` (see the rationale comment on `GameState.Hero`). This is correct for every combat reachable today, because per `combat-flow-lld §15` the undo gate closes at combat start whenever a token is revealed (face-down enemy drawn, garrison flipped, ruins turned) — verified against MKUE Rulebook pp. 8–9 that base combat rolls no dice, so the reveal is the only gate trigger — and wounds are drawn only afterward. **Gap:** a **face-up rampaging enemy** (orc marauder / draconum) provoked with no die roll reveals nothing, so per §15 undo stays OPEN through that whole combat; there, the wounds drawn in Assign-Damage would survive an undo of the block-phase card plays (half-rollback), since `RestoreSnapshot` (used per card-play via `UndoController.ExecuteUndo`) does not restore `Hero`. **Unreachable today:** dev-combat trips the gate at combat start (`PlaceholderMainMenu.cs:186`), and rampager provocation is Epic-4 story `4-5-trigger-rampaging-orc-...`. **Resolve when 4-5 lands** — the right model is likely to unwind the whole combat on undo of the trigger-move (combat as an undoable unit), NOT field-level snapshotting of `Hero`. [scripts/core/GameState.cs Hero property, scripts/core/UndoController.cs:64]
- **`Hero` has no draw pile / Deed deck (only `Hand` + `DiscardPile`)** — the minimal `Hero` introduced in 3-4 models `Armor`, `Units`, `Hand`, and `DiscardPile`, but not the hero's deck of unplayed cards. Not needed for Assign-Damage (Wounds come from the shared Wound pile, not the deck), and consistent with the current codebase — `DeckManager` (the UI-side hand) also has no draw pile, and deck reshuffle is already an open deferral (see 1b-6). The full Hero-owns-deck model (`DrawPile`/Deed deck + reshuffle, and the `DeckManager` → `Hero` unification) lands with deck-cycling / deck-building (Epic 7 turn-loop draw step and Epic 5 progression). Flagged by John during 3-4 review. **Still open after 3-4b** — 3-4b added `WoundsDrawn`/`WoundsToHand` and the live panel, but deliberately did NOT touch this unification; the on-screen `DeckManager` hand still does not show combat Wounds (the readout reads `CombatResult.WoundsDrawn`, sourced from `Hero.Hand`, instead). [scripts/core/Hero.cs, scripts/deck/DeckManager.cs]
- **Knockout / knockdown + Paralyze-vs-hero not implemented** — Phase 3 Option B stops after drawing Wounds (`combat-flow-lld §10.1` Option-B tail deferred): no `IsKnockedOut`, no cross-assignment `newWoundsInHand ≥ hand size` threshold, no "discard all non-Wound cards" on KO, and no Paralyze-vs-hero hand-emptying. `CombatResult` carries no `HeroKnockedOut` field. This is the deliberate scope split — it is **story 3-5** (`3-5-enter-knockdown-when-hand-is-all-wounds`). Still true after 3-4b: `CombatResult.WoundsDrawn` is a plain count with no knockout semantics. [scripts/combat/CombatResolver.cs, scripts/combat/CombatGroup.cs CombatResult]
- **🚧 BLOCKER FOR 3-5 — `DeckManager.Hand` and `Hero.Hand` are two disconnected collections, and combat Wounds land in the invisible one.** Surfaced by John on the 3-4b on-device run (2026-07-12): he took 5 Wounds, the readout dialog said so, and **no Wound appeared in his hand**. Confirmed by grep — `HandView` renders `DeckManager.Hand`; `Hero.DrawWoundsToHand` appends to `Hero.Hand`; **nothing bridges the two**. Correct per 3-4b's explicit scope ("the two hands still coexist by design"), and 3-4b's `AcceptDialog` readout exists precisely because the Wounds are otherwise invisible. **But this is a hard prerequisite for 3-5, not a deferrable nicety:** 3-5 is *defined* as "enter knockdown when hand is all wounds", and with two disconnected hands the mechanic is unbuildable — the visible hand never receives Wounds (so knockdown would fire with no visible cause), while the invisible one accumulates them and is never cleared between combats (after two dev combats John was carrying 7 Wound cards in `Hero.Hand`). **John's decision (2026-07-12): the unification is the FIRST TASK OF STORY 3-5**, ahead of any knockdown work. Scope it as: make `Hero` the single owner of the hand, repoint `HandView`/`DeckManager` at it, and decide the between-combat lifecycle of `Hero.Hand` Wounds (they must NOT silently accumulate across combats once the hand is real). [scripts/core/Hero.cs, scripts/deck/DeckManager.cs, scripts/ui/components/HandView.cs, scripts/combat/CombatResolver.cs]
- ~~**Assign-Damage phase has no live UI (target picker + wound readout)**~~ — **RESOLVED in 3-4b**: `DamageAssignmentPanel` (bottom-sheet, mirrors `BlockTargetingPanel`'s layout) is registered as `UIBroker.DamageTargetProvider` in `PlaceholderMainMenu`; the player picks a unit or "Hero takes it" per decision step, and a post-combat `AcceptDialog` reports `WoundsDrawn`. [scripts/ui/components/DamageAssignmentPanel.cs, scripts/ui/screens/PlaceholderMainMenu.cs, scripts/broker/UIBroker.cs]

---

## Deferred from: code review of 3-4b-live-wire-assign-damage-phase (2026-07-12)

- **`CombatState.WoundsToHand` is a hand-maintained mirror of `Hero.DrawWoundsToHand`** — the counter is incremented at exactly one call site (the Option-B hero branch), adjacent to the only current `DrawWoundsToHand` call. Story **3-5** adds more wound-drawing paths (knockout "discard all non-Wound cards", Paralyze-vs-hero hand-emptying), and nothing in the type system, the tests, or the code forces whoever writes 3-5 to also bump `combat.WoundsToHand`. When they forget, the readout silently under-reports and **no test fails** — there is no test asserting `WoundsDrawn` equals the actual `Hero.Hand` delta. Note the obvious fix (move the counter into `Hero.DrawWoundsToHand`) does **not** work: `Hero` persists across combats while `WoundsToHand` is per-combat, so it would need a reset hook — reintroducing the reset-bug class the local-`HashSet` design deliberately avoids. **Resolve in 3-5** — either derive the count from a hand-size delta captured at phase entry, or add a test asserting `WoundsDrawn == Hero.Hand.Count(IsWound) - preCombatWounds`. [scripts/combat/CombatResolver.cs:146, scripts/combat/CombatState.cs]
- **A panel freed mid-`await` never completes its `TaskCompletionSource`** — if `DamageAssignmentPanel` (or `BlockTargetingPanel` / `RangedTargetingPanel`) is freed while the resolver awaits `_tcs.Task` (scene change, quit-to-menu), nothing calls `TrySetResult`. The `await` never resumes → `OnDevCombatPressed`'s `finally` never runs → `_combatInProgress` stays `true` forever (dev combat button permanently dead) and `Hero.Units` is never cleared. Unreachable today (no mid-combat scene exit exists). **Pre-existing seam property shared by all three panels** — 3-4b adds a third instance, it does not introduce the shape. This is precisely what story **3-6** (`force-quit-mid-combat-and-restore-to-exact-decision-point`) must solve, and it is the concrete form of the deeper problem: the resolver's position mid-combat (`d`, `alreadyAssigned`, the call chain) lives in a C# continuation on the heap and is **not serializable**. Guard when 3-6 lands: override `_ExitTree` to `_tcs?.TrySetCanceled()` and let `ResolveCombat` unwind. [scripts/ui/components/DamageAssignmentPanel.cs:66-74, BlockTargetingPanel.cs, RangedTargetingPanel.cs]
- **Zero test coverage for panel components** — `scripts/ui/components/**` is deliberately outside the pure-C# test project's `.csproj` globs (Godot types cannot load in the headless suite), so no panel has any automated test. Three of this review's findings (stale `QueueFree`d buttons still live for a frame, unit labels renaming across spill steps, the re-entrancy guard fabricating a damaging `HeroAbsorbs`) are exactly the class no existing test could catch — and the resolver tests would all still pass with the panel deleted entirely. Needs a scene-tree / `GutTest` integration harness (`tests/integration/` is an established convention in project-context.md but is not yet set up for panels). [scripts/ui/components/, tests/maguswarrior.Tests.csproj]
- **`Hero.cs` is in `scripts/core/`, but project-context.md's File Placement table says `scripts/hero/`** — `scripts/hero/` does not exist. Pre-existing from 3-4 (not a 3-4b defect), but 3-4b's `WoundsDrawn`/`WoundsToHand` work now leans on `Hero` from inside the resolver, so the placement is load-bearing. Either move the file or amend the table; leaving both standing means the table actively misleads Epic 5's unit/recruitment work. [scripts/core/Hero.cs, docs/project-context.md#file-placement]
- **Combat bottom-sheet panels are semi-transparent — the hex map shows through the text** — `BlockTargetingPanel` / `RangedTargetingPanel` / `DamageAssignmentPanel` use `PanelContainer`'s default stylebox, so hexagons cut across the panel text and hurt legibility. Pre-existing (visible before any 3-4b change); consciously left as-is by John on 2026-07-12 ("that's fine"). The fix is known and small: `CombatInterstitialPanel` already does it — `var bg = new StyleBoxFlat(); bg.BgColor = new Color(0f,0f,0f,0.6f); sheet.AddThemeStyleboxOverride("panel", bg);`. It is a **look** decision, not a correctness one — the map showing through may even be desirable. Revisit during a UI polish pass. [scripts/ui/components/BlockTargetingPanel.cs, RangedTargetingPanel.cs, DamageAssignmentPanel.cs]
- **i18n: ~30 UI strings outside the combat panels are still hardcoded English** — story 3-4b's review adopted the translation convention project-wide (John's decision, 2026-07-12): `scripts/core/Strings.cs` is the shared accessor, `data/strings/ui_strings.csv` is the key table, and **all four combat panels** (Interstitial, Ranged, Block, DamageAssignment) are converted. **Not yet converted:** `RestView`, `ImprovisationView`, `CardExpanded`, `CardCompact`, `StagingAreaView`, `HexMapView`, `TileCountView`. Debug-only UI (`EffectEventLogPanel`, the dev-combat button) is intentionally exempt — dev tools do not ship. Two gotchas for whoever picks this up: (1) `data/strings/ui_strings.en.translation` is a **build artifact** — after editing the CSV you must re-import (`godot --headless --import`) or the new keys render as raw `ui.foo.bar` text at runtime; (2) `AutoTranslateMode.Always` works **only** for static strings assigned verbatim to `Text` — an interpolated string is unique at runtime and can never match a key, so it must go through `Strings.Format` with `AutoTranslateMode.Disabled` (see the header comment in `Strings.cs`). **Separately deferred:** enemy names (`data/enemies.yaml`) and `AttackType`/`EnemyAbility` enum `ToString()`s are untranslated **data**, not UI — real i18n of those is a data-model change (keyed names in YAML), not a panel change. [scripts/core/Strings.cs, data/strings/ui_strings.csv, scripts/ui/components/]
- **`UnitInstance` reference identity is a load-bearing, undocumented invariant** — both `eligible.Contains(target)` (the new ineligible-unit throw) and the `alreadyAssigned` HashSet (once-per-combat enforcement) depend on `UnitInstance` being a `class` with no `Equals` override. Converting it to a `record` — plausible, since every type in `CombatContributions.cs` is one — would silently break both: `Contains` would match a *different* unit instance with identical stats, so damage could land on the wrong unit and `AssignDamage_IneligibleUnitChoice_Throws` would stop throwing. Nothing in the code or tests states this. Document the invariant (and consider an explicit `Id`) when Epic 5 gives units identity. [scripts/units/UnitInstance.cs:12]

---

## Deferred from: code review of 3-4-assign-damage-to-unit (2026-07-05)

- ~~**Broker seam cannot distinguish "hero chose to absorb" from "UI returned an ineligible unit"**~~ — **RESOLVED in 3-4b**: the seam now returns a typed `DamageChoice` (`HeroAbsorbs` / `AssignToUnit`); an `AssignToUnit` carrying a unit not in `eligible` throws `InvalidOperationException` (caught bug, not a silent hero-branch). [scripts/combat/CombatContributions.cs DamageChoice, scripts/combat/CombatResolver.cs ApplyOneDamageAssignment]
- ~~**Broker seam never surfaces the remaining `d` on a spill**~~ — **RESOLVED in 3-4b**: `PromptHeroDamageTarget` gained a `remainingDamage` parameter carrying the running post-armor `d`; `DamageAssignmentPanel` reads it directly instead of recomputing the resolver's math. [scripts/broker/UIBroker.cs, scripts/combat/CombatResolver.cs ApplyOneDamageAssignment]
- **`UnitInstance` resistance input is unnormalized/unvalidated** — a caller passing a literal `AttackType.ColdFire` in the resistances set resists *nothing* (the derivation is Fire∧Ice; a stored `ColdFire` is dead data), and the "do not store a ColdFire entry" invariant lives only in a comment. `Armor` is likewise accepted as any int (no ≥1 floor; a 0-armor unit would be wounded while absorbing nothing). Unreachable until unit definitions/data exist. Normalize/validate constructor input (expand or reject `ColdFire`; floor `Armor`) when Epic-5 unit recruitment/data lands. [scripts/units/UnitInstance.cs]

---

## Deferred from: code review of 3-3-block-incoming-damage (2026-06-30)

- **One block allocation covers ALL of a multi-attack enemy's attacks** — `ResolveBlockPhase` computes `allocated` (the hero's block for an enemy) once, then re-applies the full pool against every entry in `enemy.Definition.Attacks`; block is never consumed per attack. A multi-attack enemy (e.g. Physical 4 + Fire 4) is fully blocked by just 4 Physical block — the same points count twice. This violates LLD §9.3 ("block resources allocated to one attack cannot cover another"). Latent today: all 31 enemies in `data/enemies.yaml` have exactly one attack (verified). The real fix needs per-attack targeting — `BlockDeclaration.Target` is the enemy, not a specific attack, so there's no way to direct or consume block per attack. That's a model change beyond resolver-core scope; resolve when multi-attack enemies and per-attack block targeting are introduced (likely alongside the block live-wire 3-3b or a later enemy-abilities story). [scripts/combat/CombatResolver.cs ResolveBlockPhase]
- **`ResolveBlockPhase` is not idempotent** — the method only `Add`s to `combat.DamageAssignments`, never clearing at entry; `DamageAssignments` is cleared only in `TearDownCombatState` at end of combat. A second invocation on the same `CombatState` double-counts damage. The single-pass `ResolveCombat` flow is correct, but the method is `public` and re-runnable (relevant under the undo-gate re-run model). Not patched because a blind entry-clear would be wrong if a future ranged phase or phase callback legitimately adds a `DamageAssignment` before block — the right ownership of clearing is a small design question. Revisit when Phase 3 (assign-damage) consumes `DamageAssignments` or if phases become re-runnable. [scripts/combat/CombatResolver.cs ResolveBlockPhase]
- **`enemy.Definition.Attacks` is dereferenced without a null guard** — the per-enemy `foreach (var attack in enemy.Definition.Attacks)` throws `NullReferenceException` if `Attacks` is ever null. Controlled construction today: `EnemyLoader` always sets the list (empty at worst) and all tests pass a populated list, so unreachable. Add a guard if enemy definitions ever come from a less-controlled source. [scripts/combat/CombatResolver.cs ResolveBlockPhase]

---

## Deferred from: code review of 3-2b-live-wire-ranged-phase (2026-06-28)

- **Modifier parsing accepts any `EnemyAbility` name, not just attack modifiers** — `EnemyLoader.BuildAbilities` runs `Enum.TryParse<EnemyAbility>(mod, ignoreCase: true)` over each attack's `modifiers` list, which will silently accept `Fortified`, `Summon`, `PhysicalResistance`, `FireResistance`, `IceResistance`, or `ArcaneImmunity` as if they were modifiers, rather than validating against the intended set `{poison, brutal, swift, paralyze}`. A YAML typo that happens to match a non-modifier enum name routes through undetected. Controlled startup YAML (we author it), so low risk today; tighten to an explicit allow-list when modifier coverage expands. [scripts/combat/EnemyLoader.cs BuildAbilities]
- **Required numeric fields (`armor`, `fame`) are unvalidated and default to 0** — `YamlEnemyEntry.Armor`/`Fame` are `int` value types; a YAML entry that omits the key silently yields a 0-armor/0-fame enemy. Unlike `id` and `color` (which throw on missing/unknown), numeric fields have no presence check. Controlled startup data, so unreachable in practice; add required-field validation if enemy data is ever authored by hand at scale. [scripts/combat/EnemyLoader.cs]
- **`summon` attacks are emitted as `None`-typed `EnemyAttack` entries** — `ParseAttackType` maps `attack_type: summon` to `AttackType.None`, but `BuildAttacks` still constructs an `EnemyAttack(value, None)` and adds it to the definition's `Attacks` list (in addition to setting the `Summon` ability). Whether a summoner's placeholder attack should appear as a real (None-typed) attack or be suppressed entirely is a Phase 2–4 question — enemy-attack resolution (block/assign-damage/melee) is not implemented in this story. Revisit when summon behavior is wired. [scripts/combat/EnemyLoader.cs]
- **Loaded enemy definitions live on the UI node rather than `GameState`** — `PlaceholderMainMenu._enemyDefs` holds the parsed `EnemyLoader.ParseAll` result, while project-context "Data Access" states all runtime game data is accessed through `GameState` (loaders run once at startup; results live in `GameState`). This is sanctioned dev-trigger scaffolding — enemy-token piles are explicitly Epic 4 — but it is a real deviation from the binding rule and should move into `GameState` (or a Hero/pile owner) when token piles land. [scripts/ui/screens/PlaceholderMainMenu.cs]
- **`ResourcesChanged` is unsubscribed only on Pass; no `_ExitTree` safety net** — `RangedTargetingPanel.ShowAndAwait` subscribes `_state.ResourcesChanged += RefreshAttackDisplay` and unsubscribes only in `Complete()` (which fires solely on the Pass button). A panel freed or re-shown without Pass would leave a live delegate pointing into a disposed/duplicated `Control`. Panels are single-instance app-lifetime nodes in current scaffolding, so this is unreachable today — it is the same cross-cutting scene-teardown concern logged for `HexMapView`, `HandView`, `TileCountView`, etc. in prior reviews. Solve as one teardown pass when scene lifecycle is introduced. [scripts/ui/components/RangedTargetingPanel.cs]

---

## Deferred from: code review of 3-2-use-ranged-siege-attack-in-phase-1 (2026-06-26)

- **Type/Delivery-changing attack modifiers are not reflected in the ranged-phase survival filter, type-grouping, or resistance keys** — `ResolveRangedPhase` keys the fortification-delivery filter off pre-modifier `c.Delivery`, `GroupBy` off pre-modifier `c.Type`, and the union-resistance check off that same pre-modifier type, while `ComputeEffectiveAttack` runs the full `AttackModifiers` chain (which could in principle change `Type` or `Delivery`) before returning only `Value`. A modifier that converts Physical→Fire or Ranged→Siege would be summed under the wrong type bucket, have resistance checked against the wrong type, and be mis-gated by fortification. Not reachable today: no `ICombatAttackModifier` changes Type/Delivery — all current modifiers adjust `Value` only (LLD §8.5). Element-conversion is a real Mage Knight mechanic but isn't modeled through the value-chain yet; resolve when/if such a modifier is designed (the correct approach — apply the chain before filtering/grouping vs. modeling element changes separately — is a design decision, not a mechanical fix). [scripts/combat/CombatResolver.cs ResolveRangedPhase]
- **`AttackPool` carries raw, pre-modifier, pre-fortification-filter contributions** — every `decl.Contributions` item is appended to `combat.AttackPool` before the fortification/delivery filter and without modifier application, so the pool holds raw values for contributions that were nullified by fortification or that are Melee (illegal this phase). This append-all behavior is spec-mandated by AC7 ("appended to AttackPool for record/consistency... the resolver reads targeting from the declaration, not the pool") and is harmless today because nothing consumes `AttackPool`. Revisit the raw-vs-effective divergence and whether blocked contributions belong in the pool when a later combat phase (block/melee) actually reads `AttackPool`. [scripts/combat/CombatResolver.cs ResolveRangedPhase]

---

## Deferred from: code review of 2-5-see-remaining-tile-counts (2026-06-19)

- **`TileStock` is not persisted in the save — reload resets the count** — `SaveData` persists only `current_phase`; `TileStock` (and `WorldMap` map state) are reconstructed fresh in `PlaceholderMainMenu._Ready`. Backgrounding the app after revealing a tile (count 8→7), then relaunching, resets the HUD to `Countryside: 8` AND re-hides the revealed tile (`isRevealed: false` again). The count stays self-consistent with the reset map, but exploration progress is silently lost. Same deferral class as `WorldMap.HeroPosition` (not snapshotted). Wire map + deck state into the save/snapshot in the Epic 7 / save story. [scripts/save/SaveData.cs, scripts/map/TileStock.cs]
- **Tile counts are a hardcoded scenario placeholder decoupled from actual map inventory** — `TileStock(8, 3)` is a fixed constant in `PlaceholderMainMenu`, independent of the tiles actually placed in `_worldMap`. After revealing the single seeded `countryside-1` tile the HUD reads `Countryside: 7` even though zero further countryside tiles exist to reveal, and `Math.Max(0, …)` would silently floor (masking the mismatch) if the constant ever undershoots the real tile count. The `8/3` values are intentional — they satisfy the epic's "8 countryside + 3 core" display requirement — but the number is fiction for everything past the seeded tile. Resolved when the real tile deck lands (the deferred tile-drawing story — load `data/tiles.yaml`, shuffle, draw) plus Epic 7 scenario config; at that point `TileStock` either becomes the count facet of the real deck or is replaced. [scripts/ui/screens/PlaceholderMainMenu.cs, scripts/map/TileStock.cs]
- **`TileCountView.Changed` and `PlaceholderMainMenu`'s `TileRevealed` lambda are never unsubscribed** — `TileCountView.Initialize` does `_stock.Changed += RefreshLabel` with no `_ExitTree`/`-=`, and `PlaceholderMainMenu` does `_worldMap.TileRevealed += tile => _tileStock.RecordReveal(tile.TileType)` (an un-removable lambda with no stored reference). If a view is freed while its model outlives it, `RefreshLabel` fires on a disposed `Label`; if `_Ready` ever runs twice on a retained `_worldMap`, the lambda double-subscribes and each reveal decrements twice. Single-instance app-lifetime nodes today, so unreachable. This is the established codebase-wide pattern (`HexMapView.HeroMoved`, `HandView`/`RestView`/`StagingAreaView` `HandChanged`/`StagingChanged`, all logged in prior reviews) — solve as one cross-cutting teardown concern when scene lifecycle is introduced, not per-view. [scripts/ui/components/TileCountView.cs, scripts/ui/screens/PlaceholderMainMenu.cs]

---

## Deferred from: code review of 2-4-reveal-new-tile-by-moving-to-map-edge (2026-06-19)

- **Tile-overlap silent-overwrite cluster across the new `RevealTile` path** — `WorldMap.RevealTile` calls `_grid.Add(worldCoord, …)` and `HexGrid.Add` is `_hexes[coord] = state` (last-write-wins, never throws). `HexMapView._hexPolygons[worldCoord] = poly` is likewise last-write-wins, and in `HandleHexTap` Branch 2 (`hexState != null` → move) returns before Branch 3 (explore) is reached, so any coord present in BOTH the grid and an unrevealed tile can never be explored. If two tiles ever share a world coord: the shared hex's terrain silently flips on reveal, the first tile's polygon is orphaned (never recolored), `FindTileForCoord` returns whichever tile was placed first, and a hero standing on the shared hex has its footing terrain mutate with no notification. The current two tiles (starting + countryside-1) are verified non-overlapping, so none of this is reachable today. This extends the already-open `PlaceTile` collision-guard deferral (from the 2-1 review) to the new reveal path — resolve both together when real tile placement / placement-legality (`hex-movement-lld §5.4`) lands. [scripts/map/WorldMap.cs RevealTile, scripts/hex/HexGrid.cs:8, scripts/ui/components/HexMapView.cs Initialize/HandleHexTap]
- **Hex polygons are created only at `HexMapView.Initialize` — runtime-placed tiles are invisible-but-tappable** — `Initialize` is the only place that creates `Polygon2D` nodes (one per hex of every `PlacedTile`). `RevealTile` adds hexes to the grid and `OnTileRevealed` only recolors polygons that already exist in `_hexPolygons`; neither creates a polygon. For story 2-4 this is fine because both tiles are placed (in `BuildStartingMap`) before `Initialize` runs. But the natural next step — drawing exploration tiles from a deck and placing them at runtime — will add tiles to the `WorldMap` after `Initialize`, leaving their hexes fully interactive (in the grid / `FindTileForCoord`) yet with no rendered polygon. The tile-drawing story must add a "create polygon on `PlaceTile`/`RevealTile`" path (e.g. a `TilePlaced` event mirroring `TileRevealed`, or build polygons in `OnTileRevealed` for coords not yet in `_hexPolygons`). [scripts/ui/components/HexMapView.cs Initialize, OnTileRevealed]
- **`PixelToHex`/`HexRound` always resolve to the nearest hex — taps in the fog gutter or just off-map still act on a real hex** — there is no "did the tap land inside a rendered polygon?" test; every pixel rounds to some axial coord. With fog hexes now widening the tappable area, a tap that visually missed all hexes can still preview/commit on the nearest grid or unrevealed-tile coord. Branch 4 ("complete miss") only catches coords that are in neither the grid nor any unrevealed tile, not coords that are spatially far but mathematically nearest. Pre-existing from the 2-2 review (logged there as the swipe-registers-as-tap item and the untested `PixelToHex`/`HexRound` inverse); re-surfaced here. Add a polygon-bounds hit test or extract the transforms to a pure-C# helper with a round-trip test when next touching `HexMapView` geometry. [scripts/ui/components/HexMapView.cs PixelToHex/HexRound]

---

## Deferred from: code review of 2-3-spend-move-points-to-cross-hexes (2026-06-10)

- **`_movePath` unbounded + `ResetMovePoints`/`ClearMovePath` have no caller → cross-turn move-point leak** — `WorldMap._movePath` is only emptied by `ClearMovePath`, which nothing calls outside tests; `GameState.ResetMovePoints` likewise has no caller. With no turn-boundary reset, `_movePath` accumulates every move of the session, and once a turn loop exists, tapping the hero hex at the start of a new turn would `UndoLastMove` a previous turn's move and refund its cost into the current pool. Acknowledged in story 2-3 Dev Notes. Wire `ResetMovePoints()` + `ClearMovePath()` at turn start/end in Epic 7 (TurnManager); story 2-4 may call `ClearMovePath()` at tile reveal (the undo gate). [scripts/map/WorldMap.cs:9,48 · scripts/core/GameState.cs:56]
- ~~**`ClearMovePath` fires no event → showing preview label persists after the undo gate trips**~~ — **RESOLVED in 2-4**: `WorldMap.TileRevealed` event added; `HexMapView.OnTileRevealed` clears `_previewedHex` and hides `_previewLabel`. [scripts/map/WorldMap.cs, scripts/ui/components/HexMapView.cs]
- **Hero's own tile can't be cost-previewed; no feedback on a no-op undo tap** — the hero-hex tap is reserved for undo (AC5/AC6), so the one hex the player occupies can never show a "Move: N" preview, and tapping it with nothing to undo silently returns. By design for story 2-3; minor UX gap. Revisit during UX polish (Epic 8) — e.g. brief "nothing to undo" cue or allow long-press to inspect the current tile. [scripts/ui/components/HexMapView.cs HandleHexTap]
- **No automated coverage for `PixelToHex`/`HexRound`** — the pixel→hex inverse (tap targeting) has no unit test; any change to `HexSize`/orientation would silently break tap resolution. Originally logged in the 2-2 review; re-surfaced here. Add a focused test (or extract the math to a pure testable helper) when next touching `HexMapView` geometry. [scripts/ui/components/HexMapView.cs PixelToHex/HexRound]
- **AC7 (hero marker moves) and AC9 (StagingAreaView running total) not unit-testable** — both depend on the Godot scene tree / view subscriptions and can only be confirmed on desktop/device. Tracked as outstanding items in `epic-2-manual-test-checklist.md` for the end-of-epic manual pass.

---

## Deferred from: code review of 2-1-see-hex-map-and-hero-position (2026-06-08)

- **`HeroMoved` subscription never unsubscribed — freed-node risk on teardown** — `HexMapView.Initialize` does `_map.HeroMoved += coord => _heroMarker.Position = ...` with no `_ExitTree`/`Dispose` unsubscribe. `WorldMap` is owned by `PlaceholderMainMenu` and outlives any `QueueFree()` of the view, so firing `HeroMoved` after the node is freed would dereference a disposed `Polygon2D`. Cannot manifest in story 2-1 (single screen, app-lifetime view, no movement yet). This is the established pattern codebase-wide — `HandView.cs:85`, `RestView.cs:70`, `StagingAreaView.cs:66-67` all subscribe to model events without unsubscribing, and there is zero `_ExitTree` in `scripts/`. Should be solved as a cross-cutting concern when screen teardown is introduced, not singled out for HexMapView. [scripts/ui/components/HexMapView.cs:37]
- **`PlaceTile` silently overwrites on hex collision; no §5.4 placement-legality enforcement** — `WorldMap.PlaceTile` calls `_grid.Add(worldCoord, …)` and `HexGrid.Add` is `_hexes[coord] = state` (last-writer-wins, never throws). Two tiles sharing a world coordinate clobber silently. The real placement rules in `docs/hex-movement-lld.md` §5.4 (Countryside adjacent to ≥2 placed tiles, Core ≥2, no tile behind coastline; §5.5 replacement ≥3) are not enforced. Correct for story 2-1 (one tile, no overlap); spec explicitly defers placement/movement validation. Story 2-4 (tile reveal) must add a collision/adjacency guard — the silent-overwrite `Add` will otherwise mask misplacement bugs. [scripts/map/WorldMap.cs:21-26, scripts/hex/HexGrid.cs:8]
- ~~**`MapTile.Reveal()` does not back-propagate to `WorldMap.Grid`**~~ — **RESOLVED in 2-4**: `WorldMap.RevealTile(MapTile)` added; calls `tile.Reveal()`, populates `_grid`, calls `ClearMovePath()` (undo gate), fires `TileRevealed`. Never call `MapTile.Reveal()` directly from game code. [scripts/map/WorldMap.cs]
- **`HexState.IsRevealed` redundant/always-true** *(pending decision — may become a patch)* — two reveal flags (`MapTile.IsRevealed` per-tile, `HexState.IsRevealed` per-hex); `PlaceTile` always writes `IsRevealed: true`, so the per-hex flag is never false and has no reader. Risk of fog-of-war drift if a future story trusts it. Resolution deferred to user decision in the 2-1 review. [scripts/hex/HexState.cs:5, scripts/map/WorldMap.cs:23-25]

---

## Deferred from: code review of 2-2-tap-hex-to-preview-move-cost (2026-06-10)

- **`IsDay` participates in the per-effect undo snapshot** — AC9 required `IsDay` in `GameStateSnapshot`, and `EffectScheduler` snapshots before each effect / restores on undo. Once day/night round transitions are wired (Epic 7), an undo of an unrelated effect could roll back a day/night flip. Day/night is a round-level global, not effect-local state. No guard distinguishes the two today. Revisit during turn/round-structure implementation. [scripts/core/GameState.cs RestoreSnapshot]
- **Drag/swipe lifting over a hex registers as a tap** — `_Input` treats any `InputEventScreenTouch{Pressed:false}` as a tap with no press-position capture or movement threshold, so a swipe that ends over a hex previews it. Harmless today (no pan/camera gesture exists); revisit when panning is added in story 2-3+. [scripts/ui/components/HexMapView.cs:55]
- **No automated coverage for `PixelToHex`/`HexRound` inverse math** *(reconfirmed from 2-2 single-reviewer pass)* — the riskiest new code is untested because the transforms are `private static` on a Godot `Node2D`, unreachable from the pure-C# test project. Recommend extracting hex transforms into a pure-C# `MagusWarrior.Hex` helper in a follow-up so a `PixelToHex(HexToPixel(c)) == c` round-trip test is possible. [scripts/ui/components/HexMapView.cs:96]

---

## Deferred from: code review of 1b-7-improvisation-card-gives-resource-determined-by-current-game-phase (2026-06-06)

- **`CardExpanded` Play-enable can stage an illegal primary effect for non-Improvisation multi-type cards** — `CardExpanded.Open` now enables Play when *any* of `{Unpowered.EffectType} ∪ AlternateEffectTypes` is legal in the current phase (added for Improvisation). But only Improvisation has an intercept in `HandView.OnPlayRequested`; every other multi-type card (cards.yaml lines 352, 627, 727, 828, 942 — e.g. cards with `effect_type: [Move, Influence, Combat]` or `[Influence, Combat]`) falls through to the normal stage path, which stages `card.Unpowered.EffectType` (the *primary*). If the primary is illegal in the current phase but an alternate is legal, Play is enabled and the card stages with an illegal primary effect (or shows a wrong running total). Unreachable today: those cards aren't in the test hand and no phase transition beyond Movement/Rest exists. Becomes live once the turn loop (Epic 7) introduces combat/interaction phases. Fix: phase-aware effect-type selection at stage time, or a multi-type-play prompt analogous to ImprovisationView, for any card with `AlternateEffectTypes`. [scripts/ui/components/CardExpanded.cs, scripts/ui/components/HandView.cs]
- **`ImprovisationRule.GetPhaseEffect` / `CanPlay` are production-dead — two sources of truth for phase legality** — `GetPhaseEffect` (phase → single effect) and `CanPlay` are called only from unit tests. Production code re-derives legality independently: `ImprovisationView.BuildResourcePanel` lists all four options and disables illegal ones via `PhaseGate.IsLegal`, and `ImprovisationView.Activate` uses `_deck.Hand.Count == 0` instead of `CanPlay`. The view's 4-option-panel approach is spec-sanctioned (effect-lld: "show options grayed-out, not hidden"), but the duplicated legality logic can drift from `GetPhaseEffect` in future edits. Consider making `GetPhaseEffect` the single source consulted by both `BuildResourcePanel` and `CardExpanded`. Low priority. [scripts/cards/ImprovisationRule.cs, scripts/ui/components/ImprovisationView.cs]
- **`ImprovisationView` overlay has no cancel/escape affordance** — once `Activate` makes the full-rect overlay visible, the only way to dismiss it is to complete a discard + resource selection. A player who opens Improvisation by mistake is forced through the whole flow. Pre-discard rollback is possible (`Activate` already calls `_deck.ReturnCard` on the empty-hand path), but post-discard there is no `DeckManager` "un-discard" operation, so a true Cancel needs rollback semantics for the discarded card. UX addition beyond story 1b-7 scope; revisit alongside the overlay-coordination work for the Epic 7 turn loop. [scripts/ui/components/ImprovisationView.cs]

---

## Deferred from: code review of 1b-6-declare-rest-turn-and-discard-per-rest-rules (2026-06-06)

- **Exhaustion auto-discard uses magic string `DiscardCard("wound")`** — `RestView.OnDeclareRestPressed` discards by literal id `"wound"`, but `IsExhaustion` keys off `CardType.Wound`. Works today because every Wound has `Id = "wound"` (`WoundCard.Create`), but if Wound ids ever diverge the discard fails while `IsExhaustion` is true → `_endRestButton` stays disabled → unfinishable Rest. Fix: discard the first card found by `Type == CardType.Wound` (align with the type-based check). [scripts/ui/components/RestView.cs]
- **`OnDeclareRestPressed` mutates phase to Rest before the fallible discard, with no re-check or rollback** — phase is set to `Rest` first; the exhaustion `DiscardCard` can (in principle) fail, leaving the player soft-locked (End Rest disabled, Declare disabled by `inRest`). Also relies entirely on the button's `Disabled` state rather than re-checking `RestRule.CanDeclareRest`. Unreachable in the current scaffold (IsExhaustion guarantees a Wound; hand is static before Rest). Add a re-validate + phase rollback when the turn loop (Epic 7) makes phase transitions dynamic. [scripts/ui/components/RestView.cs]
- **HandView Rest-gate checks only `card.Unpowered`, not a Powered effect** — `OnCardTapped`'s Rest guard computes `hasLegalPlay` from `Unpowered` only; a card with a Powered-only Rest-legal effect would be wrongly blocked from opening CardExpanded. Powered play is not wired up yet (Power button always disabled), so unreachable. Revisit when powered play lands. [scripts/ui/components/HandView.cs]
- **Declaring Rest doesn't reset HandView interaction state (open panel / pre-staged cards)** — RestView is a sibling node with no reference to `HandView._expandedPanel` or the staging manager, so declaring Rest leaves an open CardExpanded visible and lets cards staged in Movement be committed during Rest (`OnCommitRequested` has no phase gate). The reachable Play-during-Rest path is closed by the `OnPlayRequested` guard patch; this remaining coordination gap wants a turn coordinator (Epic 7 TurnManager) that resets interaction state on phase change. Supersedes the implementation-time "stale Play signal during Rest" note below. [scripts/ui/components/RestView.cs, scripts/ui/components/HandView.cs]

---

## Deferred from: implementation of 1b-6-declare-rest-turn-and-discard-per-rest-rules (2026-06-06)

- **Latent: stale Play signal during Rest** — if a ghost tap from a prior `CardTapped` signal propagates after `OnDeclareRestPressed` (same Godot signal-replay risk from 1b-1), a non-Heal card could reach `OnPlayRequested` during Rest phase. `OnCommitRequested` does not check PhaseGate before calling `BuildEffect`; a ghost play during Rest would stage an illegal effect. Mitigation: add a `PhaseGate.IsLegal` check at the top of `OnCommitRequested` when phase transitions become more frequent. Harmless today (no observable ghost-play source in the current scaffold). [scripts/ui/components/HandView.cs]
- **Deck reshuffle not implemented** — `DeckManager.DiscardPile` accumulates cards from rest discards, card plays (future), and other discard actions but is never reshuffled back into the deck. Any card draw effect or "draw to hand limit" mechanism would attempt to draw from an empty deck without triggering a shuffle. Implement `DeckManager.ReshuffleDiscardIntoDeck()` when deck cycling is needed (end-of-turn draw step, Epic 7). [scripts/deck/DeckManager.cs]
- **RestView.HandChanged subscription never unsubscribed** — `RestView.Initialize` subscribes `_deck.HandChanged += RefreshView` with no matching `_ExitTree` unsubscribe. Same pattern as existing `HandView.HandChanged` deferral (logged from 1b-1 and 1b-3 reviews). Add `_ExitTree` override when scene lifecycle gets more complex. [scripts/ui/components/RestView.cs]

---

## Deferred from: code review of 1b-5-cannot-tap-wound-card (2026-06-04)

- **`OnPlaySidewaysRequested` wound guard null-card fall-through relies on `PlayCard` fail-safe without a comment** — the guard `card is not null && card.Type == CardType.Wound` correctly skips when `card` is null (stale ghost-tap), letting execution fall through to `PlayCard` which returns `Result.Fail`. This is the same fail-safe the pre-existing code relied on, and it works. However a future reader adding wound-specific handling between the guard and `PlayCard` could silently re-open the gap. The Dev Notes explain the intent but the code itself has no bridging comment at the fall-through point. Low priority; add a brief comment ("null = stale tap, handled by PlayCard below") when next touching `OnPlaySidewaysRequested`. [scripts/ui/components/HandView.cs]

---

## Deferred from: code review of 1b-4-undo-staged-cards-before-new-information-revealed (2026-06-04)

- **`DeckManager.ReturnCard` has no Id-uniqueness / membership guard** — `ReturnCard` does `list.Add(card)` without checking whether a card of the same `Id` is already in `Hand`. The hand's "each `Id` appears at most once" invariant — relied on by `OnCardTapped`, `OnPlayRequested`, and `PlayCard` via `FirstOrDefault`/`FindIndex(c => c.Id == ...)` (which silently bind to the first match) — is maintained today only by the LIFO Stage/Unstage convention, not by `ReturnCard` itself. Unreachable through the current undo path (a card is removed from hand on Stage and re-added on Unstage, preserving uniqueness), but the new public API is one careless caller away from a silent wrong-card bind. Fix when hand-invariant hardening is tackled: guard `ReturnCard` against duplicate Ids, or centralize the uniqueness invariant. [scripts/deck/DeckManager.cs]
- **Integrated `OnUndoRequested` flow has no automated coverage** — the combined unstage-then-return handler lives in `HandView` (Godot `Control`), which cannot be compiled into the pure-C# test project. The two halves are unit-tested in isolation (`Unstage_*` in StagingManagerTest, `ReturnCard_*` in DeckManagerTest) but their composition in the signal handler is verifiable only on-device. Same Godot/test-project boundary already logged for `StagingAreaView`/`HandView` in 1b-1/1b-3. Manual on-device verification recommended before closing Epic 1b. [scripts/ui/components/HandView.cs]

---

## Deferred from: code review of 1b-3-stage-multiple-cards-and-see-running-totals (2026-06-03)

- **Running totals diverge from committed effect for multi-stat & `choose_one` cards** — `StagingManager.GetTotals` sums all four `EffectSpec` fields per staged card; `HandView.BuildEffect` applies only the single `entry.EffectType` field. They agree for single-stat cards (the whole test hand) but diverge for any multi-field spec. `choose_one` cards (Rage, Determination) load with all-zero stat fields because `CardLoader.ConvertSpec` ignores `choose_one`, so staging Rage shows "Attack: 0" and Commit applies nothing. Both halves are spec-sanctioned (AC1 sums EffectSpec, AC3 switches on EffectType, Dev Notes defer `choose_one`). Fix — unified "what this staged card contributes" representation + the attack-or-block choice prompt — belongs to the `choose_one` story (1b-7 / combat). Surfaces the moment Rage is added to the Epic 1b demo hand. [scripts/deck/StagingManager.cs, scripts/ui/components/HandView.cs]
- **Unsupported `EffectType` staged card removed from hand then silently dropped on Commit (data loss)** — `OnPlayRequested` removes the card from hand at stage time, but `BuildEffect` returns `null` at commit for `Special`/`Heal`/`Banner`/`Mana`/`Crystal`/`Fame`/`Reputation`/`AttackSiege`; the entry is logged + skipped, then `Clear()` drops it with no effect and no recovery. Reachable for `Special` cards (Play button enabled because `PhaseGate` maps `Special` → `Any`), none in the test hand today. The remove-from-hand-first atomicity model (correct for sideways) has a staging-specific gap: buildability isn't checked until commit. Fix: validate buildability at stage time before removing from hand, or return un-buildable cards to hand. [scripts/ui/components/HandView.cs]
- **No phase-legality gate inside `OnPlayRequested`** — the `CardExpanded` Play button is phase-gated, but the signal handler stages without a `PhaseGate`/`PhaseValidator` check (contrast `OnPlaySidewaysRequested`'s null-guard). Defense-in-depth gap reachable only via replayed/stale signals; `CurrentPhase` is always `Movement` today. Add a handler-side guard when phase transitions land. [scripts/ui/components/HandView.cs]
- **`StagingChanged` / `HandChanged` subscriptions never unsubscribed** — `StagingAreaView.Initialize` (`StagingChanged += Refresh`) and `HandView.Initialize` (`HandChanged += RefreshHand`) have no matching `-=`/`_ExitTree`. Managers outlive the view nodes; a freed view's handler would fire on a freed Godot node on screen rebuild. Single-instance screen today. Same lifecycle item as the 1b-1 `HandChanged`-unsubscribe deferral. [scripts/ui/components/StagingAreaView.cs, scripts/ui/components/HandView.cs]
- ~~**`BuildEffect` / `PhaseGate` attack-distance coverage mismatch**~~ — **RESOLVED in 3-2**: `BuildEffect` now maps `EffectType.AttackSiege => new AttackEffect(spec.Attack, EffectType.AttackSiege, AttackElement.Physical)`. `PhaseGate`, `BuildEffect`, and the sideways-null path are now consistent for all three attack-distance types. [scripts/ui/components/HandView.cs]

---

## Deferred from: code review of 1b-2-play-card-sideways-for-basic-resource (2026-06-02)

- **Wound cards can be played sideways — rules violation** — Rulebook p4: "Wound cards cannot be played in any way." `OnPlaySidewaysRequested` has no `CardType` guard; only the `PlaceholderMainMenu` test-hand filter (which strips Wounds) currently hides it. Owned by Story 1b-5 (wound-untappable treatment). Rules-confirmed against `MKUE_Rulebook_BOOKLET.pdf`. [scripts/ui/components/HandView.cs:92]
- **Record value-equality landmine on `GameStateSnapshot`/`EffectFiredEvent`** — Both are positional records whose `AttackPool`/`BlockPool` members are `IReadOnlyDictionary`. Auto-generated `Equals`/`GetHashCode` therefore use reference equality, not content equality. Any future code that compares snapshots, puts `EffectFiredEvent`s in a `HashSet`/`Dictionary`, or uses `Distinct` over the event log (e.g. undo-dedup) will silently get wrong results. No current consumer does this. Fix when snapshot comparison is introduced: custom `Equals` or a value-equal dictionary wrapper. [scripts/core/GameEventLog.cs:7]
- **Dead Sideways affordance in non-play phases** — `CardExpanded` enables the Sideways button unconditionally, but `SidewaysRule.GetEffect` returns `null` for `CombatRanged`/`CombatStart`/`CombatAssignDamage`/`Rest`/`EndOfTurn`/`Any`. Tapping Sideways in those phases silently no-ops (logs a debug line, card stays in hand). Disable the button when `GetEffect` returns null. Not reachable until phase transitions/HUD exist (`CurrentPhase` defaults to `Movement` and never changes today). Note: rulebook p9 confirms cards cannot be played sideways during Ranged/Siege, so the `null` for `CombatRanged` is rules-correct — only the dead UI affordance needs addressing. [scripts/ui/components/HandView.cs:94]
- **Minor pool robustness** — `GameState.AddAttackPoints` accepts any `EffectType` as the "distance" axis of the `AttackPool` key (a dedicated distance enum would prevent miswired `(Move, Physical)` entries); the `Add*` mutators don't guard `amount > 0`, so a 0-amount play would leave a meaningless `(key, 0)` entry. Not reachable (sideways is always 1). Low priority. [scripts/core/GameState.cs:32-39]
- **`PriorityQueue` tie-break instability** — `EffectScheduler.Enqueue` uses priority with no FIFO tie-break; same-priority effects resolve in nondeterministic order. Pre-existing scheduler property, unreachable while sideways produces a single leaf effect. [scripts/cards/effects/EffectScheduler.cs]
- **Full atomic sideways play — carry to the UIBroker story** — The 1b-2 review applied a partial fix (reorder: `PlayCard` first, effect only on success) that prevents double-banking and orphaned resources. True atomicity — card leaves hand **and** resource is granted, or neither, with rollback if a future awaitable effect fails mid-flight — requires `Hand` (currently in `DeckManager`) to become part of the `GameState` snapshot so the event-log/undo system covers it. Address when UIBroker introduces real awaitable interactions and `async void` is revisited. [scripts/ui/components/HandView.cs:92]

---

## Deferred from: code review of 1b-1-tap-card-to-see-options (2026-05-31)

- **`HandView` never unsubscribes `HandChanged` from `DeckManager`** — `Initialize` subscribes `deck.HandChanged += RefreshHand` with no matching `_ExitTree` unsubscribe. If `HandView` is freed before `DeckManager`, the delegate dangles and `RefreshHand` fires on a freed node. Not triggered in 1b-1 (both owned by `PlaceholderMainMenu` for the whole session). Add `_ExitTree` override when scene lifecycle gets more complex. [scripts/ui/components/HandView.cs:21]
- **`QueueFree`'d `CardCompact` nodes can still emit `CardTapped` before frame deletion — ghost tap risk** — `RefreshHand` calls `QueueFree` on old nodes but they remain alive until end of frame. A tap event queued against an old node fires `OnCardTapped` after the hand has been rebuilt, potentially triggering a `First()` lookup on a card no longer in the hand. Not triggerable in 1b-1 (hand set once at startup, never changes). Fix when `SetHand` starts being called dynamically: disconnect signals before `QueueFree`, or use a generation counter to discard stale taps. [scripts/ui/components/HandView.cs:27-35]
- **`SetHand` fires `HandChanged` before `handView.Initialize` subscribes — silent ordering dependency** — `PlaceholderMainMenu._Ready()` calls `SetHand(testHand)` then `handView.Initialize(...)`. The `HandChanged` event fires during `SetHand` with no subscribers; `Initialize` then calls `RefreshHand()` directly (correct). Works today, but the ordering is a silent assumption: if `SetHand` is ever moved after `Initialize`, or another subscriber expects to catch the first `HandChanged`, it will silently miss it. Consider calling `SetHand` after `Initialize` as the canonical pattern. [scripts/ui/screens/PlaceholderMainMenu.cs:47-50]

---

## Deferred from: code review of 1a-3-full-effect-event-log-in-inspector (2026-05-30)

- **`RefreshDisplay` uses `QueueFree()` + immediate `AddChild()`** — old labels are queued for end-of-frame deletion while new ones are added synchronously, so the container transiently holds both within the frame. Spec (AC3) prescribed `QueueFree()`, and Godot frees old nodes before the next redraw so no visible duplication occurs for this debug tool. Adopt `RemoveChild` + `QueueFree` (or `Free()`) if the panel ever becomes player-facing or refreshes rapidly (Undo-mashing compounds the overlap). [scripts/ui/debug/EffectEventLogPanel.cs:20-32]
- **`FireTestEffect` is `async void` and not awaited** — if any effect ever performs a real `await`, the panel can render a stale/empty log with no re-refresh path. By design today: `async void` is the documented `[Conditional("DEBUG")]` exception and `MoveEffect.Execute` is synchronous (`Task.FromResult`), so the event is appended before `_Ready()` returns. Revisit when effects perform real awaits. [scripts/ui/screens/PlaceholderMainMenu.cs:27, scripts/cards/effects/EffectScheduler.cs:20]
- **New Godot UI behavior has zero automated coverage** — refresh formatting, undo wiring, and the 5-tap toggle are not exercised by any test because `EffectEventLogPanel.cs` (a Godot `Panel`) cannot be compiled into the test project. Inherent to the Godot/test-project boundary. Manual on-device verification recommended before closing Epic 1a. [scripts/ui/debug/EffectEventLogPanel.cs]

## Deferred from: code review of 1a-2-undo-last-event (2026-05-29)

- **Snapshot captures scalars only** — `GameStateSnapshot` holds `CurrentPhase` + `MovePointsThisTurn`. Undo silently fails to restore any other mutable state. By documented design (incremental contract), but the snapshot AND `RestoreSnapshot` MUST be grown in lockstep every time `GameState` gains a mutable field (reputation, fame, hand, board position). A missed field = a silent half-rollback. [scripts/core/GameState.cs, scripts/core/GameEventLog.cs]
- **Undo grouping semantics for triggered-effect chains undecided** — Sequential LIFO undo composes correctly today (event_B.before = event_A.after, so popping B then A walks back cleanly). But one `UndoLastEvent` reverses one effect, not a whole user-action causal chain as a unit. A transaction/grouping decision is needed before any effect returns triggered effects. Not exercised today — `MoveEffect` returns empty `Triggered`. [scripts/cards/effects/EffectScheduler.cs]
- **Event appended regardless of `EffectResult.Success`** — A no-op or failed effect still produces a log entry (with `StateBefore == StateAfter`) and still re-enqueues triggered effects. `EffectFiredEvent` records no success flag and no after-state. Revisit when effects can actually fail (today `MoveEffect` always returns `Ok()`). [scripts/cards/effects/EffectScheduler.cs]
- **Triggered-effect context not phase/type-validated** — Triggered effects are re-enqueued with their `TriggeredEffect.Ctx` as-is; a stale or illegal `Phase`/`EffectType` is logged without validation. Ties to the same future triggered-effect work. [scripts/cards/effects/EffectScheduler.cs]
- **EffectScheduler exception handling** (also tracked from 1a-1) — a thrown `Execute` kills the queue with no rollback of the snapshot just taken. [scripts/cards/effects/EffectScheduler.cs]

## Deferred from: code review of 1a-1-define-and-fire-effect (2026-05-29)

- **Relative `data/cards.yaml` path will fail on Android device** — process CWD differs from project root on the export; needs `ProjectSettings.GlobalizePath` or `FileAccess` rework. Ties to story-0.2 device blocker. [scripts/core/GameState.cs:9, scripts/cards/CardLoader.cs]
- ~~**`AlternateEffectTypes` never populated from YAML**~~ — **PARTIALLY RESOLVED in 1b-7**: `AlternateEffectTypes` IS populated by `CardLoader` from multi-item YAML `effect_type` lists. `CardExpanded.Open` now checks `AlternateEffectTypes` so the Play button enables correctly for Improvisation in all four valid phases. Remaining: `PhaseValidator.ValidatePhase`'s alternate-effect path still has no call site (dead code); `LegalPhases` IS populated by CardLoader but also has no call site. Address when `PhaseValidator` is wired into the play/commit pipeline. [scripts/cards/CardLoader.cs, scripts/ui/components/CardExpanded.cs]
- **`EffectScheduler` lacks exception handling, cancellation, cycle detection** — a thrown effect kills the queue; a self-triggering effect loops forever; no `CancellationToken`. Intentionally minimal per spec; revisit as real effect chains land. [scripts/cards/effects/EffectScheduler.cs]
- **`PriorityQueue<T,int>` is not stable** — equal-priority effects dequeue in undefined order; will cause flaky behavior once priorities matter. Add insertion-counter secondary key. No conflicts today (all priorities 0). [scripts/cards/effects/EffectScheduler.cs]
- **`EffectHookRegistry` sorts on every call and is not thread-safe** — stub today, no hooks registered. Address before first real `IEffectHook` lands. [scripts/cards/effects/EffectHookRegistry.cs]
- **`GameState.MovePointsThisTurn` has no reset path** — name says "this turn" but value accumulates forever. End-of-turn structure out of scope for 1a-1. [scripts/core/GameState.cs:11]
- **`CardDefinition` exposes public mutable setters** — `{ get; set; }` is required by YamlDotNet's default constructor binding; the "definitions" are not actually immutable. Revisit with `init` accessors + custom YamlDotNet deserializer. [scripts/cards/CardDefinition.cs]
- **`PhaseGate.GamePhase.Any` semantics ambiguous when passed as `currentPhase`** — `Mana`/`Crystal`/`Special` are `Any` in the table; if a caller ever passes `Any` as the current phase the result is asymmetric and undocumented. Not exercised today. [scripts/cards/effects/PhaseGate.cs:27-29]
- **Test coverage gaps** — no tests exercise `PhaseValidator`, `EffectHookRegistry`, the `Powered` spec branch, multi-type cards, `StringOrListConverter`'s sequence form, scheduler priority/triggered/exception paths. Spec asked for the 5 tests delivered; expand as system grows. [tests/unit/EffectSystemTest.cs]
- **CardLoader test path uses fragile `../../../../data/cards.yaml`** — four `..` segments assume current TFM/output depth. Walk-up search or `Directory.GetParent()` loop would be more robust. [tests/unit/EffectSystemTest.cs]
- **YamlDotNet `16.3.0` version duplicated** across `maguswarrior.csproj` and `tests/maguswarrior.Tests.csproj`. Centralize via `Directory.Packages.props`. [maguswarrior.csproj, tests/maguswarrior.Tests.csproj]
- **Tests csproj uses per-file `<Compile Include="../scripts/...">`** — every new effect needs a manual entry here or tests silently skip it. Switch to a glob (`Include="../scripts/cards/**/*.cs"`) with Godot-class excludes. [tests/maguswarrior.Tests.csproj]

## Deferred from: code review of 0-3-add-ui-string-via-string-table (2026-05-29)

- **Test path hardcoded depth** — `I18nTest.cs` resolves CSV via `AppContext.BaseDirectory + "../../../../"`. Breaks if test output structure differs (e.g., CI publish subdirectory). Consider a walk-up search or `Directory.GetParent()` loop as a more robust alternative. [tests/unit/I18nTest.cs:10-11]
- **Weak CSV value assertion** — `CsvContainsPlaceholderTitleKey` only checks that the value is non-empty. A stricter assertion against the exact canonical string would catch accidental truncation or corruption. [tests/unit/I18nTest.cs:26]
- **CSV encoding undeclared** — `ui_strings.csv` uses an em dash (`—`, U+2014) with no BOM or `.editorconfig` charset rule. A non-UTF-8 tool re-save corrupts the character silently. Add `charset = utf-8` to `.editorconfig` for `*.csv`. [data/strings/ui_strings.csv]
- **`data/strings/` naming convention undocumented** — Intended naming pattern (`ui_strings.<locale>.csv`), subdirectory policy, and how to add new locales should be noted in a comment or doc before the string table grows. [data/strings/]
- **CRLF brittleness in header test** — `Assert.Equal("keys,en", firstLine)` fails on Windows CI due to trailing `\r`. Guard with `.TrimEnd('\r', '\n')` if the project ever runs tests on Windows. [tests/unit/I18nTest.cs:16]
- **`First()` throws on empty CSV** — `File.ReadLines(CsvPath).First()` throws `InvalidOperationException` (not a test failure message) if the file is empty. Pre-read to a list and assert non-empty first for a clearer failure. [tests/unit/I18nTest.cs:16]
- **Key prefix collision risk** — `StartsWith("ui.placeholder_menu.title,")` matches any key beginning with that string. The comma delimiter is sufficient now, but if a key like `ui.placeholder_menu.title_extended` is added without the comma guard being updated, the wrong row is matched. [tests/unit/I18nTest.cs:24]

## Deferred from: code review of 0-1-build-and-deploy-to-galaxy-s21 (2026-05-21)

- ~~**GameState.CurrentPhase private set**~~ — **RESOLVED in 1b-6**: `public void SetPhase(GamePhase phase)` added to `GameState`. CombatResolver and TurnManager will call `SetPhase` for all phase transitions. [scripts/core/GameState.cs]
- ~~**GamePhase missing combat phases**~~ — ✅ **RESOLVED (2026-06-20)**: All five combat phases (`CombatStart`, `CombatRanged`, `CombatBlock`, `CombatAssignDamage`, `CombatMelee`) are in the committed enum. [scripts/core/types/GamePhase.cs]
- **GamePhase missing ActionDeclaration** — `turn-structure-lld.md §2.6` defines `GamePhase.ActionDeclaration` as the turn-level phase between Movement and whatever the hero does next (combat, site, etc.). This is NOT a combat phase — it belongs alongside `Movement` and `Rest` in the top-level turn flow. Not needed for Epic 3; must be added before the first Epic 4 story that implements action declaration. [scripts/core/types/GamePhase.cs, docs/turn-structure-lld.md §2.6]
- **GameDebug.Inspect(object?) boxes value types in release builds** — `[Conditional("DEBUG")]` strips the body but arguments are still evaluated and boxed in release. Revisit when Inspect is called from hot paths; consider wrapping call sites in `#if DEBUG` or changing signature to `string`. [scripts/core/GameDebug.cs]
- **GameConstants.MaxHandSize vs Hero.UnmodifiedHandSize** — Two independent sources of truth for starting hand size. When Hero is built (Epic 3+), ensure UnmodifiedHandSize is initialized from GameConstants.MaxHandSize and there is a single canonical source. [scripts/core/GameConstants.cs]
- **SiteType single Unknown value** — Placeholder only. When Epic 4 site types are added, ensure all switch expressions over SiteType have exhaustive cases or a discard arm to prevent CS8509 compile errors. [scripts/core/types/SiteType.cs]
- **AllowUnsafeBlocks enabled with no stated reason** — May be a Godot SDK requirement; if so, add a comment explaining why. If not required, remove it. No active unsafe code exists today. [maguswarrior.csproj]
- **AC 1/6/7 hardware deployment unverified** — Build success (AC 1, 6) and Galaxy S21 launch (AC 7) are runtime outcomes that the agent could not verify. Story should not be fully closed until user confirms the APK builds and runs on device.
- **Result<T> default(T) on Fail footgun** — `Result<int>.Fail(...)` returns Value=0; callers that read Value without first checking IsSuccess silently receive a valid-looking value. Enforce the check-first contract via convention or add a `ThrowIfFailure()` helper when the pattern proves error-prone in practice. [scripts/core/Result.cs]

## Deferred from: story 2-3 design (2026-06-10)

- **Movement lock on action declaration — implementation contract not yet specified** — `hex-movement-lld.md §4.1 rule 2` says all movement must be completed before taking any action. `turn-structure-lld.md §8.2` defines the Movement → Action → EndOfTurn flow. What is NOT documented: (a) how the movement lock is enforced in code (e.g. a `GamePhase.ActionDeclaration` transition gates `HexMapView` from accepting commits), and (b) that the action declaration itself is undoable until new information is revealed — so if a player starts an interact or assault, they can undo back to the movement phase as long as no enemy token has been drawn, no tile revealed, etc. `GamePhase.ActionDeclaration` is defined in `turn-structure-lld.md §2.6` but is not yet in `scripts/core/types/GamePhase.cs`. **Must be specified before the first Epic 4 story that implements action declaration (assault keep, visit village, etc.)** — add the movement-lock enforcement and undo gate contract to `site-interaction-lld.md` (not yet written) and the relevant story spec. [docs/hex-movement-lld.md §4.1, docs/turn-structure-lld.md §8.2, scripts/core/types/GamePhase.cs]

## Deferred from: code review of story 2-0 (2026-06-08)

- **D1 — InputLock non-thread-safe bool** — `InputLock` uses a plain `bool` with no Interlocked/memory barrier. Safe today (effects resolve synchronously via `Task.FromResult`; Godot dispatches signal handlers on the main thread, so acquire/release never cross threads). When UIBroker introduces genuinely awaitable effects whose continuations may resume off the main thread, add a memory barrier or guarantee a main-thread continuation. [scripts/ui/InputLock.cs]
- ~~**D2 — Partial InputLock coverage**~~ — ✅ **RESOLVED in story 3-0 (2026-06-19)**: `OnPlayRequested` now acquires `_lock` in try/finally (immediate-resolve model). `OnResourceSelected` in ImprovisationView also now acquires `_lock` in try/finally. The staging model was removed entirely — there is no longer a staged-but-unlocked window for card plays.
- **D3 — Silent dropped actions** — `TryAcquire`-false branches `return` with no log or UI feedback, in a codebase that otherwise logs liberally. Add `Log.Debug("[UI]", "...input locked, action dropped")` to each guard's false branch to aid field diagnosis of shared-lock drops. [all InputLock-guarded handlers]
- **D4 — Improvisation discard step holds no lock** — Between `Activate` and `OnResourceSelected` the Improvisation flow holds no lock; it relies on the full-screen `ZIndex=5` overlay (PanelContainer, MouseFilter=Stop) to block cross-view input. If the overlay becomes non-blocking/non-full-screen, a cross-view action could interleave mid-flow. Document/assert the overlay's input-blocking invariant or guard `Activate`. [scripts/ui/components/ImprovisationView.cs]
- **D5 — Sideways/commit play not atomic** — `_deck.PlayCard` removes the card before the effect resolves; a synchronous throw between `PlayCard` and `ResolveAll` would spend the card with no effect applied. Pre-existing; tracked for the UIBroker/atomic-rollback story (Hand is not yet in `GameStateSnapshot`). [scripts/ui/components/HandView.cs OnPlaySidewaysRequested]

## Deferred from: code review of story 3-0 (2026-06-19)

- **`OnUndoRequested` checks `_lock.IsLocked` but never acquires it** — the synchronous undo handler guards on the flag but does not `TryAcquire`/`Release`; only a real race once effects become genuinely awaitable (UIBroker). Same class as D1/D2 above. Have undo acquire the lock in try/finally when await-windows land. [scripts/ui/components/HandView.cs OnUndoRequested]
- ~~**`StagingManager` is now dead production code with a live test**~~ — ✅ **DECISION (2026-06-20)**: Reuse in combat confirmed. `StagingManager`/`StagedEntry` will serve as the staging layer for combat card plays (attack pool accumulation). No deletion; wire into CombatResolver in Epic 3. [scripts/deck/StagingManager.cs, tests/unit/StagingManagerTest.cs]
- ~~**Undo button always enabled, no disabled-state feedback**~~ — ✅ **RESOLVED (2026-06-20)**: `StagingAreaView.Refresh()` now sets `_undoButton.Disabled = !_state.UndoController.CanUndo`; fires on every `ResourcesChanged` event including after undo. [scripts/ui/components/StagingAreaView.cs]
- **Undo recovers cards by Id, not by played instance** — `OnUndoRequested` looks up the source card in `_state.Cards` (master list) and `RecallFromDiscard` uses `FindIndex` by Id. Harmless while `CardDefinition` is value-by-Id, but recalls an arbitrary match on duplicate Ids and returns null for cards absent from the master list (e.g. Wounds, currently unplayable). Store the actual played `CardDefinition` instance in `EffectFiredEvent` when hand-state/snapshot hardening is tackled. [scripts/ui/components/HandView.cs OnUndoRequested, scripts/deck/DeckManager.cs RecallFromDiscard]
- **`UndoGateCrossed` has no unsubscribe on teardown** — `_state.UndoGateCrossed += _worldMap.ClearMovePath` in bootstrap with no matching `-=`; a latent handler leak / freed-object risk once scene reload exists. Single bootstrap today. [scripts/ui/screens/PlaceholderMainMenu.cs]
- **Card-undo / hero-move undo are two uncoordinated systems** — card-play undo (EventLog snapshot) does not capture WorldMap hero position, so undoing a Move card after walking with its points rewinds the points but not the board (free move). Surfaced as a decision in the story 3-0 review findings; if not resolved there, this is the long-term fix: unify the two undo paths onto one ordered stack, or add hero position to `GameStateSnapshot`. [scripts/ui/components/HandView.cs, scripts/ui/components/HexMapView.cs]

## Deferred from: code review of story-3-1 (2026-06-20)

- **No event unsubscription in StagingAreaView/HandView** — both subscribe to GameState events (PhaseChanged, ResourcesChanged, HandChanged) with no `_ExitTree` detach. GameState outlives the Controls. Pre-existing pattern across all views; warrants a systemic UI-lifecycle pass, not a single-story fix. [scripts/ui/components/StagingAreaView.cs, HandView.cs]
- **RestoreSnapshot fires PhaseChanged but CombatState sub-state is outside the snapshot** — undoing across a combat-phase boundary would rewind CurrentPhase while ActiveEnemies/DamageAssignments (on CombatState, not in GameStateSnapshot) stay populated → phase/combat desync. Combat undo is not enabled until story 3-6 (force-quit mid-combat restore); resolve there. [scripts/core/GameState.cs RestoreSnapshot]
- **CardCompact.IsCardPlayable ignores card.LegalPhases** — the greyness hint uses only PhaseGate.IsLegal on effect types, while PhaseValidator checks LegalPhases first at play time. A LegalPhases-restricted card could render bright but be rejected on play (or vice versa). Latent: no card sets legal_phases today. Revisit when the first LegalPhases card is added. [scripts/ui/components/CardCompact.cs]
- **CombatResolver stub semantics (partially resolved in 3-2)** — ~~`ResolveStartOfCombat` `AddRange` without `Clear` (re-entry duplicates enemies)~~ **FIXED in 3-2**: `Clear()` now precedes `AddRange`. Remaining open: `FirePhaseCallbacks` snapshots the callback list via `ToList()` before iterating (a callback that enqueues another for the same phase is silently skipped); `BuildResult.HeroWon` is only meaningful once all phases run (currently true only if `ActiveEnemies` was empty before combat started). Fix both when block/melee phases land. [scripts/combat/CombatResolver.cs, CombatState.cs]
- **HasResistanceTo has no test coverage** — non-trivial branching incl. the derived ColdFire rule (Fire ∧ Ice). Will be exercised when block phase (3-3) consumes it; add coverage then. [scripts/combat/EnemyToken.cs]
- **EnemyTokenInstance.WoundCount/IsDestroyed unwired** — TakeWound/Destroy/IsDestroyed exist but nothing reads them or removes from ActiveEnemies; two parallel notions of "dead" (list removal vs flag) until reconciled in 3-3/3-4. [scripts/combat/EnemyToken.cs]

## Deferred from: code review of story-3.3b (2026-07-01)

- **Block panel dump-all / no per-attack split UI** [scripts/ui/components/BlockTargetingPanel.cs:OnBlockPressed] — panel declares the entire available block pool against one tapped attack index and disables the rest; a player cannot split one pool across a multi-attack enemy's attacks without interleaving card plays. Per-attack resolver model is done; this is the panel affordance. Latent until multi-attack enemies exist (all 30 current enemies single-attack). *(This item is currently a [Decision] in the story — see resolution note there.)*
- **Synchronous TCS continuation in re-entrancy guard** [scripts/ui/components/BlockTargetingPanel.cs:ShowAndAwait] — `_tcs` lacks `RunContinuationsAsynchronously`; a stranded awaiter would resume the old block phase mid-re-arm. Defensive-only (guarded by `_combatInProgress`); identical pattern in RangedTargetingPanel — fix both panels together.
- **Out-of-range / mismatched AttackIndex silently drops block** [scripts/combat/CombatResolver.cs:ResolveBlockPhase] — a BlockDeclaration whose AttackIndex matches no real attack is silently ignored (attack takes full damage). Not reachable from the panel; BlockDeclaration has no validation. Controlled construction today.
- **BlockTargetingPanel subscription survives abnormal scene teardown** [scripts/ui/components/BlockTargetingPanel.cs] — ResourcesChanged unsubscribed only in Complete()/re-arm; GameState is a plain C# object not freed with the tree, so a panel freed mid-await → disposed-node callback → ObjectDisposedException. No _ExitTree/PREDELETE unsubscribe. Cross-cutting teardown concern (same as RangedTargetingPanel, logged in 3-2b review).
