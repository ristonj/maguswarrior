# Team overrides for gds-code-review skill.
#
# Model auto-switching was REMOVED 2026-06-19. Previously a UserPromptSubmit hook
# in ~/.claude/settings.json forced Opus during /gds-code-review and this on_complete
# restored Sonnet at the end. That loop rewrote the model on every prompt and fought
# manual /model choices, so it was retired. Default model is now a plain Sonnet in
# settings.json; switch to Opus manually with /model when you want a review in Opus.

[workflow]
on_complete = ""
